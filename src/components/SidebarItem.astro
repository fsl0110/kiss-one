---
import { HTMLAttributes } from "astro/types";

interface Props extends HTMLAttributes<"a" & "div"> {
  isAnchor?: boolean;
}

const { isAnchor = false, ...attrs } = Astro.props;

const Element = isAnchor ? "a" : "div";
---

<Element {...attrs}><slot /></Element>
